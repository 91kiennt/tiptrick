import 'package:tiptrick_game/auth/_auth_export.dart';

class CommonStyle extends StatelessWidget {
  const CommonStyle({Key key}) : super(key: key);

  @override
  Widget build(BuildContext context) {
    return Container();
  }
}
